[package]
name = "tauri-shell"
version = "0.1.0"
edition = "2021"

[dependencies]
tauri = { version = "2.0.0-beta.9", features = ["api-all"] }

[build-dependencies]
tauri-build = { version = "2.0.0-beta.9", features = [] }

[package.metadata.tauri]
allowlist = { all = true }
